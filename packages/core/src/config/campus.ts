/**
 * Canonical default campus used for onboarding, role requests, and profile
 * bootstrap. This must remain in sync with `supabase/seed/campuses.sql`.
 */
export const DEFAULT_CAMPUS_CODE = 'supercampus-main';
