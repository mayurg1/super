import { EmptyState } from '@supercampus/shared';

export function EmptyFeed(): React.ReactElement { return <EmptyState icon="✦" title="No posts yet" description="Be the first to share an update with your campus." />; }
