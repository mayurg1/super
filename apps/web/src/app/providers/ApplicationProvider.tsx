import { createContext, useContext, useMemo, type ReactElement, type ReactNode } from 'react';
import { useAuthorization, useAuth, useProfile, useRoleRequests } from '@supercampus/supabase';
export interface ApplicationContextValue { loading:boolean; ready:boolean; error:string|null; retryInitialization:()=>Promise<void> }
const Context=createContext<ApplicationContextValue|null>(null);
export function ApplicationProvider({children}:{children:ReactNode}):ReactElement{const a=useAuth(),p=useProfile(),z=useAuthorization(),r=useRoleRequests();const value=useMemo(()=>({loading:a.loading||p.loading||z.loading||(a.authenticated?r.loading:false),ready:!a.loading&&(!a.authenticated||(p.exists&&!p.loading&&z.ready&&!r.loading)),error:p.error??z.error??r.error,retryInitialization:async()=>{await p.refreshProfile();await z.refreshAuthorization();await r.refresh();}}),[a.loading,a.authenticated,p.loading,p.exists,p.error,p.refreshProfile,z.loading,z.ready,z.error,z.refreshAuthorization,r.loading,r.error,r.refresh]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useApplication():ApplicationContextValue{const v=useContext(Context);if(!v)throw Error('useApplication must be used inside ApplicationProvider');return v}
